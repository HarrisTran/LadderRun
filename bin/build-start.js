module.exports = function(options /* see above cocos docs*/) {
    Editor.log(`Xây dựng bắt đầu:`)
    // return a promise if you need to do something asynchronous
    //return new Promise((resolve, reject) => {resolve()})
  }